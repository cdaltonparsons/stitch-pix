// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyDC6LiUVNTKHCC-hE-9OU-cFK8ERdS0nrc",
    authDomain: "stitch-pix.firebaseapp.com",
    databaseURL: "https://stitch-pix-default-rtdb.firebaseio.com",
    projectId: "stitch-pix",
    storageBucket: "stitch-pix.appspot.com",
    messagingSenderId: "477269489921",
    appId: "1:477269489921:web:08fa3b3c5bbde5c8d9c300",
    measurementId: "G-7M2S5K62JQ"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
